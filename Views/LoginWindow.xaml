<Window x:Class="SkillProfiAdmin.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="300" Width="400"
        Background="#F3F4F6" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Логин -->
        <StackPanel Grid.Row="0" Margin="0,30,0,10">
            <TextBlock Text="Логин:" FontSize="16" Margin="0,0,0,5"/>
            <TextBox x:Name="UsernameTextBox" Width="350" Height="30" />
        </StackPanel>

        <!-- Пароль -->
        <StackPanel Grid.Row="1" Margin="0,10,0,10">
            <TextBlock Text="Пароль:" FontSize="16" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Width="350" Height="30"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="2" Margin="0,10,0,0">
            <Button Content="Войти" Click="LoginButton_Click" Width="100" Height="40"/>
            <Button Content="Отмена" Click="CancelButton_Click" Width="100" Height="40"/>
        </StackPanel>
    </Grid>
</Window>
